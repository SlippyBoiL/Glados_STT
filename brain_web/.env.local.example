# API base URL when running Next.js dev server separately from FastAPI.
# Production: leave empty — static UI is served from FastAPI on the same origin (port 8888).
NEXT_PUBLIC_API_URL=http://localhost:8888

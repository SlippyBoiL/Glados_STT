{
  "facts": [
    {
      "id": "proxmox",
      "keywords": ["proxmox", "pve", "hypervisor", "vm", "qemu", "lxc", "192.168.0.129"],
      "text": "Primary hypervisor is Proxmox VE at 192.168.0.129 (SSH on port 22, web UI http://192.168.0.129:8080). Prefer checking Proxmox before assuming VMs or lab services are down."
    },
    {
      "id": "cad_scale",
      "keywords": ["cad", "scale", "fusion", "solidworks", "measurements", "mm"],
      "text": "CAD work should default to millimeters unless the user explicitly requests imperial units."
    }
  ]
}
